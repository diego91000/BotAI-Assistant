# Aucune clé n'est nécessaire si tu t'authentifies avec `ant auth login` :
# le client Anthropic() lit automatiquement le profil OAuth local.
# Sinon, décommente la ligne suivante :
# ANTHROPIC_API_KEY=sk-ant-...
